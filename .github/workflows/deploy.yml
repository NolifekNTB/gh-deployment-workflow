name: My First Workflow

on:
  push:
   branches:
     - main
   paths:
     - "index.html"

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Hello, GitHub Actions!"
